import { ContentGeneratorPanel } from "@/components/content-generator-panel";

export const metadata = {
  title: "Content Generator — SEO Analyzer",
  description: "Create content personas and generate blog posts in their unique voice and style.",
};

export default function ContentGeneratorPage() {
  return <ContentGeneratorPanel />;
}
